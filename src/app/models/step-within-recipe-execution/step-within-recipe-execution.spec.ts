import { StepWithinRecipeExecution } from './step-within-recipe-execution';

describe('StepWithinRecipeExecution', () => {
  it('should create an instance', () => {
    expect(new StepWithinRecipeExecution()).toBeTruthy();
  });
});
